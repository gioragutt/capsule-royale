{
  "name": "capsule-royale-server",
  "author": "",
  "license": "ISC",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && DEBUG=game:* node .",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "lint-fix": "npm run lint -- --fix"
  },
  "devDependencies": {
    "@types/debug": "^4.1.5",
    "@types/node": "^13.5.0",
    "lerna": "^3.20.2",
    "rimraf": "^3.0.0",
    "tslint": "^6.1.1",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "debug": "^4.1.1"
  }
}
